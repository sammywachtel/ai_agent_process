# Knowledge Base (Fallback)

This directory is only used when BEADS is **disabled** in `quality-config.json`.

When BEADS is enabled, knowledge lives in `.beads/knowledge/` — that's the primary store shared with metaswarm's `/prime` command.

See `process/knowledge-base.md` for schema and usage.
