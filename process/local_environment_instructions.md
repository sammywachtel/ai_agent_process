# Local Environment Instructions

Keep this file short. Only include what's different about this project — don't repeat standard AP workflow steps. Agents read this before every workflow phase, so bloat here wastes context on every run.

---

## Pre-Execution Setup

Commands or checks to run before any `ap_exec` implementation work.

<none>

## Multi-Repository Configuration

Branch checking, repo mapping, and coordination for polyrepo projects. If this project is a single repo, leave as `<none>`.

<none>

## Release Modifications

Custom arguments, multi-project release ordering, or additional release steps. If standard `/ap_release` works as-is, leave as `<none>`.

<none>

## Validation Extensions

Extra validation commands beyond the scoped validation script. If the standard hook validation is sufficient, leave as `<none>`.

<none>

## Notes

Any other project-specific context that affects how agents work in this environment.

<none>
